实验要求：


1. 动态创建一个CPU类的数组，数组中有两个CPU对象元素。分别设置两个CPU对象的参数，删除整个对象数组。
2. 仿照例6-18（p222）创建cpu的动态数组类ArrayOfCpu，创建动态数组类ArrayOfCpu的对象cpuArray，
   分别设置类中对象数组中元素的值,并将设置后的属性值输出出来。

   在主函数中：
   创建动态数组类ArrayOfCpu的对象cpuArray1，设置对象数组中的两个对象元素cpu1和cpu2的值；
