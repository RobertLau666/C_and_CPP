219.231.164.200 yangfeistu

1、课后3-8

2、枚举类型CPU_Rank表示cpu的型号，枚举类型CPU_Rank的元素为i3，i5，i7

要求：（1）定义CPU_Rank类型的变量；
      （2）为变量初始化（值可以是i3，i5，i7中的任意一个），并输出该变量值；
      （3）修改枚举元素的初始值，令i5=3，为变量赋值（i5或i7），输出变量值，注意值的变化；
      （4）新定义一个变量，输入一个整数（值为任意枚举元素常量对应的整数），将其赋给新变量，输出该变量值；
      （5）尝试直接为枚举元素赋值；尝试直接为变量通过键盘输入值。