1、在例6-18（p222）的基础上增加重载+，-两个运算符，实现两个ArrayOfPoints对象的Points数组中对应元素的坐标加减操作。函数返回类型是ArrayOfPoints

2、在主函数中：
   创建动态数组类ArrayOfPoints的两个对象，使其内部数组的size大小相等。对这两个对象做+，-运算。将运算后的数组中元素的坐标依次输出出来。（需要在类中增加print函数）


注：由于涉及到类中包含动态数组，因此也要在类ArrayOfPoints类中重载“=”这个赋值运算符。原因与定义深复制构造函数相同，否则会发生系统清除一块已经被清除过的动态申请的内存。重载方法可参照p356的重载“=”运算符。

